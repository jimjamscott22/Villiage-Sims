# VillageSim Milestone 3 Implementation Plan

**Goal:** Data-driven building placement with ghost preview, validation, costs/refunds, and snapshot rendering.

**Branch:** `jimjamscott22/milestone-3-building-placement-8d49`

## Tasks

1. Add `src-tauri/data/buildings.json` and Rust catalog/buildings/resources modules.
2. Introduce `mpsc` `SimCommand` channel; drain each tick; wire place/validate/demolish/viewport.
3. Extend `TickSnapshot` with buildings + resources; viewport-cull buildings.
4. Tauri IPC: `get_catalog`, `validate_placement`, `place_building`, `demolish`.
5. Frontend: types, BuildMenu, ghost, draw buildings, Canvas build mode, browser-demo world.
6. Tests + browser smoke; update `progress.md` / `AGENTS.md`; open PR.
