# VillageSim Milestone 8 Implementation Plan

**Goal:** Economy loop — gather, harvest, storage, mill→bakery recipes, hauling, ResourceBar.

**Branch:** `jimjamscott22/milestone-8-economy-2b4a`

**Design:** `docs/superpowers/specs/2026-07-24-milestone-8-design.md`

## File map

- Create: `src-tauri/src/sim/economy.rs`, `src-tauri/src/sim/nodes.rs`
- Create: `src/ui/ResourceBar.tsx`
- Modify: `resources.rs`, `catalog.rs`, `buildings.rs`, `buildings.json`, `jobs.rs`, `agents.rs`, `world.rs`, `mod.rs`, `snapshot.rs` (if needed)
- Modify: `types.ts`, `BuildMenu.tsx`, `App.tsx`, `demoWorld.ts`, tests, `progress.md`, `AGENTS.md` if needed

## Tasks

1. Design doc (done) + this plan
2. Resources + flour; stockpile rename; economy inventory helpers
3. Catalog recipes; mill/bakery/granary data; Building inventory fields
4. Job kinds Gather/Haul/Produce; resource nodes
5. World: harvest, gather, produce, haul dispatch + derived totals
6. Frontend ResourceBar + demoWorld parity
7. Tests, verify, progress, PR

## Acceptance

Grain from farm → granary → mill → bakery → eaten with no manual job assignment; ResourceBar live; gather adds wood/stone.
