# VillageSim Milestone 7 Implementation Plan

**Goal:** Utility AI (Eat/Sleep/Work/Socialize/Wander + hysteresis) and five villagers.

**Branch:** `jimjamscott22/milestone-7-utility-ai-2d15`

## File map

- Create: `src-tauri/src/sim/utility.rs`
- Create: design doc `docs/superpowers/specs/2026-07-24-milestone-7-design.md`, this plan
- Modify: `agents.rs`, `jobs.rs`, `resources.rs`, `world.rs`, `mod.rs`, `snapshot.rs`, commands if move gains id
- Modify: `demoWorld.ts`, `App.tsx`, `Canvas.tsx`, `types.ts`, tests, `progress.md`

## Tasks

1. `utility.rs` scoring + hysteresis unit tests
2. Extend `AgentState` / `MovePurpose` / villager `current_action`
3. Multi-villager world + decide/begin wiring; Eat/Sleep/Socialize ticks
4. Frontend demo parity + villager click selection
5. Tests, browser smoke, progress, PR

## Acceptance

Five villagers; farm workers tend crops; hunger drives eating from stock without job thrash; sleep at night; no action flicker.
