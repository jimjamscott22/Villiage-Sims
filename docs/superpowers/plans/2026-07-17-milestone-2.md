# VillageSim Milestone 2 Implementation Plan

**Goal:** Seeded procedural island terrain + pannable/zoomable camera, keeping the M1 20 Hz sim pipe and interpolated villager.

**Tech add:** `noise` crate on the Rust side. No new frontend dependencies.

## Constraints

- Complete only M2 from `docs/villagesim-spec.md`. No buildings, pathfinding, needs, jobs, or persistence.
- Terrain still sent once via `get_terrain`; never in tick payloads.
- Default world: `128×128` tiles, 32px/tile, seed `42`.
- Zoom clamp `0.25…4.0`; cursor-anchored wheel zoom; middle-drag + edge-scroll pan.
- Branch: `jimjamscott22/milestone-2-terrain-camera-8d49`.

## File Map

- Create: `docs/superpowers/specs/2026-07-17-milestone-2-design.md`
- Create: `docs/superpowers/plans/2026-07-17-milestone-2.md` (this file)
- Create: `src-tauri/src/sim/terrain.rs`
- Modify: `src-tauri/src/sim/world.rs`, `src-tauri/src/sim/mod.rs`, `src-tauri/src/commands.rs`, `src-tauri/src/lib.rs`, `src-tauri/Cargo.toml`
- Create: `src/render/camera.ts`, `src/render/camera.test.ts`
- Modify: `src/render/drawTerrain.ts`, `src/render/Canvas.tsx`, `src/state/transport.ts`, `src/state/types.ts`
- Modify: `progress.md`, `AGENTS.md`, `README.md` as needed

## Tasks

1. Add `noise` dependency and `terrain.rs` generator with reproducibility tests.
2. Replace `World::checkerboard` with `World::generate`; update lib setup to 128×128 seed 42; keep orbiting villager; update world tests.
3. Add `set_viewport` command + AppState storage.
4. Implement `camera.ts` (+ unit tests for zoom-at-point stability).
5. Update Canvas for viewport sizing, camera input, transform blit; expand terrain colors.
6. Update browser-demo transport to generate a deterministic island (same thresholds, demo-quality noise).
7. Verify with `cargo test`, `npm test`, `npm run build`, browser smoke; update `progress.md`.
