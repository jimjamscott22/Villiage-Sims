# VillageSim Milestone 5 Implementation Plan

**Goal:** Villager needs decay; completed farms advertise TendCrops jobs; villager claims, walks, and works; VillagerPanel shows hunger via `get_villager_detail`.

**Branch:** `jimjamscott22/milestone-5-needs-jobs-34b3`

## Constraints

- Complete only M5 from `docs/villagesim-spec.md` and the M5 design doc.
- Keep M4 pathfinding and right-click move orders.
- Do not add crops, clock UI, utility AI, eating/sleeping, or hauling.

## File map

- Create: `src-tauri/src/sim/needs.rs`, `src-tauri/src/sim/jobs.rs`
- Create: `src/ui/VillagerPanel.tsx`
- Create: `docs/superpowers/specs/2026-07-19-milestone-5-design.md`, this plan
- Modify: `agents.rs`, `world.rs`, `mod.rs`, `commands.rs` (sim + Tauri), `snapshot.rs`, `lib.rs`
- Modify: `src/state/types.ts`, `transport.ts`, `demoWorld.ts`, `App.tsx`, Canvas selection as needed
- Update: `progress.md` when M5 demos

## Tasks

1. `needs.rs` + unit tests for decay and happiness.
2. `jobs.rs` — JobBoard advertise/claim/release/remove_site + scoring.
3. Extend Villager/AgentState; wire advertise on Complete, claim from Idle, Working cycle; demolish cleanup.
4. `get_villager_detail` command + Tauri handler.
5. Frontend VillagerPanel + transport/demoWorld parity.
6. Tests + browser smoke; update `progress.md`; open PR.

## Acceptance

Place a farm → it completes → villager walks to an adjacent tile and enters Working. VillagerPanel shows hunger decreasing over time.
